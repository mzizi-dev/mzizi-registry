/**
 * GENERATED by scripts/generate-skills.mjs — do not edit.
 *
 * The skills this deployment serves, inlined from `@nyuchi/mzizi-skills` at
 * build time. There is no database copy and no sync step: bumping the package
 * is a commit, and the commit is what changes what agents read.
 *
 * Regenerate with `pnpm skills:generate`; `pnpm skills:generate:check` is the
 * CI gate that fails when this file and the installed package disagree.
 */

export interface GeneratedSkill {
  name: string;
  description: string;
  body_mdx: string;
  source: string;
}

export const SKILLS_VERSION = "0.5.1";

export const SKILLS: readonly GeneratedSkill[] = [
  {
    name: "simplify",
    description:
      "Simplify code and maximise reuse the Mzizi way — before adding a component, style, or page, and during any refactor: reuse before build (search the registry first), styles as N1 tokens, compose pages from N2/N3, collapse duplication into CVA variants, cn() composition, right-altitude imports, delete-first, and read counts/tokens/doctrine live. Companion to the get_simplify_guidance MCP tool.",
    body_mdx:
      "# Simplify — code that reads like the system\n\n**The simplest change is the one you don't write.** Reuse before you build, compose\nbefore you implement, and let one source own each decision. Use this before adding a\ncomponent, a style, or a page — and during any refactor.\n\n> This skill is the narrative form of the Mzizi MCP tool `get_simplify_guidance`, which\n> returns the same ruleset as structured data. Pull it live when you want the machine-readable\n> checklist; read this when you want the reasoning.\n\n## The rules\n\n1. **Reuse before build.** Search the registry first — `search_components` /\n   `list_components` over the MCP, or `GET https://mzizi.dev/api/v1/ui`. Install via the\n   shadcn CLI; never reimplement a primitive that already exists.\n2. **Styles are tokens.** Never hardcode a colour, radius, spacing, or motion value —\n   consume `var(--…)`. N1 is the only layer allowed to define CSS values, so a rebrand is\n   one edit. New tokens go in `:root` + `.dark` + the `@theme` block.\n3. **Compose, don't implement.** A page (N6) is a composition of N2 primitives and N3 brand\n   components — never inline a `<button>`, a card, or an SVG. Pull spinners, skeletons, and\n   badges from N2.\n4. **Variants over copies.** Collapse near-duplicate components into one CVA variants block.\n   One `cva()` per primitive; express differences as `variant` / `size` props, not new files.\n5. **One composition path.** Compose class names through `cn()`; extract shared logic into\n   `lib/` or a hook. No string-concatenated classes, no logic duplicated across files.\n6. **Right altitude.** Keep each component self-contained, importing only from the layer\n   below on its strand. A primitive that reaches up into page code can't be installed on its\n   own.\n7. **Delete first.** Prefer removing over adding — dead code, unused props, redundant\n   wrappers, commented-out blocks. Less code is less to break, read, and keep true.\n8. **Live data, not copies.** Read counts, tokens, and doctrine live (`/api/v1/stats`, the\n   generated palette, the MCP). A baked-in number or duplicated token guarantees drift.\n\n## Checklist\n\n- [ ] Searched the registry before building?\n- [ ] All styles are tokens (`var(--…)`), zero hardcoded values outside N1?\n- [ ] Page is pure composition of N2/N3, no inlined primitives?\n- [ ] Variation expressed as CVA variants, not near-duplicate files?\n- [ ] All class names through `cn()`, shared logic extracted once?\n- [ ] Each file self-contained and installable, importing only downward?\n- [ ] Deleted more than you added where you could?\n- [ ] Counts, tokens, and doctrine read live, not hardcoded?\n\n## See also\n\n- MCP tool `get_simplify_guidance` — the same ruleset as structured data.\n- `get_ubuntu_doctrine` — the philosophy the rules serve.\n- The `scaffold-component` skill — how to add a component the right way when reuse genuinely\n  isn't possible.",
    source: "mzizi-tools/mzizi-skills/skills/simplify",
  },
  {
    name: "discoverability",
    description:
      "SEO + AIO for the bundu ecosystem: how to guarantee machines can find and render a site — social/messaging link previews (OpenGraph), search engines, and AI agents. Covers the 'if the machine can't see it, it doesn't exist' convention, the brand-agnostic Mzizi hive OG-image template (dynamic per page: title + description + site icon), Next.js + Astro/Starlight wiring, APCA-checked text colours, and a pre-ship checklist. Use when a shared link shows no preview or when adding a new route/site.",
    body_mdx:
      '# Discoverability — SEO + AIO for the bundu ecosystem\n\n> **Covenant:** _If the machine can\'t see it, it doesn\'t exist._\n\nDiscoverability is the machine-facing surface of every bundu-ecosystem site — the\npart crawlers, social/messaging scrapers, and AI agents read. It is **cross-cutting**,\nnot a component you install: it is a genetic-code **convention** ("every surface ships\na title, a description, a dynamic preview image, structured data, and an\nagent-readable index") base-paired to the components that satisfy it (the OG template,\n`llms.txt`, JSON-LD helpers). A surface a machine can\'t parse may as well not exist —\nand a **missing social preview is the single biggest discoverability no-go**, because\nit\'s the first thing a human sees when a link is shared.\n\nAll values here are the current doctrine; the database (`mzizi_db →\ncomponent_documents`, served by the MCP at `mcp.mzizi.dev/mcp`) is the source of\ntruth — re-pull before assuming.\n\n## The number-one failure: a missing OG image\n\nThe most common — and most damaging — bug is a link that unfurls with **no preview\nimage at all**. The canonical cause is metadata that points at an image file that\ndoesn\'t exist:\n\n```ts\n// ❌ WRONG — points at a static file that isn\'t in /public, so every scraper 404s\nopenGraph: { images: ["/og-image.png"] }\ntwitter:   { images: ["/og-image.png"] }\n```\n\nWorse, an explicit `images` array **overrides** a framework\'s dynamic OG route, so\nthe good image never gets a chance. **Rule: never hardcode `openGraph.images`.** Let\nthe framework\'s file-convention / endpoint serve a _generated_ image, and set an\nabsolute base URL so scrapers get an absolute URL.\n\n## The Mzizi "hive" OG card\n\nOne template, every route, every brand. The card is:\n\n- **Deep-night ground** (`#1B1A17`) with a faint flat-top **honeycomb hex lattice**\n  (white at ~5% — texture, not noise) and a few solid **mineral accent cells**\n  (cobalt / gold lead, plus tanzanite / malachite / copper for warmth).\n- The **site icon on top, above the title** (brand recognition first), then the\n  title, an optional eyebrow, the SEO description, and the domain.\n- **Left-aligned** (reads better than centred at thumbnail size).\n- **Brand-agnostic:** the icon is a parameter, so the same template serves nyuchi\n  (the bee), mukoko (the swarm), bundu, shamwari — each with its own mark + domain.\n  It picks up the _site\'s own icon_, never a hardcoded one.\n\n### Colours are APCA-checked\n\nOG text is read at thumbnail size, so contrast matters. Verified against the\n`#1B1A17` ground (APCA reverse polarity; design-system floor is **Lc 75 large /\nLc 90 body**):\n\n| Role                             | Hex       | APCA Lc | Verdict |\n| -------------------------------- | --------- | ------- | ------- |\n| Title                            | `#F5F5F4` | 92      | ✓ body  |\n| Secondary (eyebrow, description) | `#E5E3DE` | 81      | ✓ large |\n| Domain (gold)                    | `#FFD740` | 76      | ✓ large |\n\nDo **not** use dim greys like `#B2AFA8` (only Lc 50) for OG text — it looks fine on a\nmonitor and vanishes in a messaging thumbnail. Re-run APCA when changing any pair.\n\n## Implementation — Next.js (App Router)\n\nThe portal ships the reference template at `lib/og/mzizi-og.tsx`\n(`renderMziziOg({ title, eyebrow, description, iconPath, domain })`). Wire it via the\nfile convention so metadata stays dynamic:\n\n```tsx\n// app/opengraph-image.tsx  (root)  — and app/<section>/[slug]/opengraph-image.tsx\nimport { renderMziziOg, OG_SIZE, OG_CONTENT_TYPE } from "@/lib/og/mzizi-og"\nexport const runtime = "nodejs"          // reads the icon PNG off disk\nexport const size = OG_SIZE\nexport const contentType = OG_CONTENT_TYPE\nexport default function Image() {\n  return renderMziziOg({ title: "mzizi" })   // per-page: pass that page\'s title + description\n}\n```\n\n```ts\n// app/layout.tsx  — set metadataBase, DO NOT set openGraph.images / twitter.images\nexport const metadata = {\n  metadataBase: new URL("https://mzizi.dev"),\n  openGraph: { type: "website", siteName: "Mzizi", title: "Mzizi", description: "…" },\n  twitter:   { card: "summary_large_image", title: "Mzizi", description: "…" },\n  // No `images` — the opengraph-image.tsx route supplies an absolute, hashed URL,\n  // and the twitter card falls back to it.\n}\n```\n\nFor dynamic routes, add `opengraph-image.tsx` alongside `page.tsx` and pull the\nentity\'s title + description (e.g. from the registry) so the shared link unfurls with\n_that_ page\'s identity.\n\n## Implementation — Astro / Starlight (static site)\n\nA statically-built site (deployed as static assets) generates OG images **at build\ntime**:\n\n1. A build-time image endpoint, `src/pages/og/[...slug].png.ts`, with\n   `getStaticPaths()` over the docs collection; render each page\'s title +\n   description with **satori** → SVG → rasterise with **sharp** → PNG.\n2. Override Starlight\'s `Head` component (`components.Head` in `astro.config`) to add\n   `<meta property="og:image" content={new URL(`/og/${slug}.png`, site)} >` per page.\n3. Use the same hive design: honeycomb + the site\'s icon + the brand mineral\n   (nyuchi docs → **gold**).\n\nKeep the site\'s runtime dependency-light; satori + sharp are build-only.\n\n## Beyond the image — the rest of the convention\n\n- **Title + meta description** on every route (unique, page-specific).\n- **Structured data**: schema.org JSON-LD for the page\'s primary entity.\n- **`llms.txt`** at the site root — the machine-readable index for LLMs.\n- **`robots.txt` + `sitemap.xml`** — generated, not hand-maintained.\n- **Canonical URLs** absolute (a set `metadataBase` / `site`).\n- **Agent-readiness**: an MCP server-card at `/.well-known/mcp/server-card.json`\n  where the surface exposes an MCP endpoint.\n\n## Pre-ship checklist\n\n- [ ] Sharing the URL in Slack / WhatsApp / iMessage / X renders a preview **image**.\n- [ ] The OG image is **generated** (dynamic), carries the page\'s title + description + the site icon, and its URL is **absolute**.\n- [ ] No hardcoded `openGraph.images` / `twitter.images`.\n- [ ] OG text colours pass APCA (Lc ≥ 75 large / ≥ 90 body).\n- [ ] `metadataBase` / `site` is set; canonical URL is absolute.\n- [ ] Title + meta description are unique per route.\n- [ ] `llms.txt`, `robots.txt`, `sitemap.xml` present; JSON-LD on primary entities.\n- [ ] Validate the card in a scraper (e.g. opengraph.xyz) after deploy.\n\n_The whole helix is Mzizi — and discoverability is the convention that makes each\nsurface legible to the machines that carry it. Ndiri nekuti tiri._',
    source: "mzizi-tools/mzizi-skills/skills/discoverability",
  },
  {
    name: "bundu-design",
    description:
      "Ecosystem-level brand: the Bundu parent identity (copper, wilderness), the verified brand constellation (every brand's mineral, voice, meaning, URL), and the Ubuntu pillars + principles. Use for cross-brand materials and brand-voice decisions.",
    body_mdx:
      "# Bundu — _The Ecosystem_\n\n**Bundu** (Shona: _wilderness_) is the parent identity of the Bundu ecosystem — the complete ecosystem\nbuilt by Nyuchi Africa. Three pillars: **mukoko** (consumer super app), **nyuchi** (infrastructure &\nenterprise), and the **sister brands** (specialist verticals) — one identity, one design system, one\nopen data commons.\n\nAll values here are verified against `nyuchi_design_db → component_documents` (collections\n`styling-ecosystem`, `genetic-code-ubuntu-pillars`, `genetic-code-ubuntu-principles`), doctrine\n**v4.1.0**. **The database is the source of truth** — re-pull before assuming; doctrine evolves.\n(Notably: principles were renamed in v4.x — use the set below, not older Shona-named lists. One\nknown DB error, corrected by the founder: the `styling-ecosystem → bundu` row carries `bundu.family`\nand family framing — the correct URL is **bundu.org** and the correct framing is **the Bundu\necosystem**. Trust this file over the DB for those two fields until the row is fixed.)\n\n## Bundu's own identity\n\n- **Mineral:** `copper` — `#BF5A36` light / `#FF8A65` dark · connection, foundation, stewardship\n- **Meaning:** Wilderness · **Language:** Shona · **URL:** <https://bundu.org>\n- **Voice:** visionary, grounded, inclusive\n- **Role:** the ecosystem — the layer that holds the whole, the commons, governance, philosophy\n- **Wordmark:** `bundu`, lowercase always, Noto Serif 600 (per ecosystem rule)\n- **No standalone Bundu logo mark is registered in the design DB yet** — that's an open identity\n  item. Until one exists, ecosystem-level materials use the wordmark in copper, optionally with\n  Mukoko's Swarm mark when the subject is the product family. Don't invent a Bundu mark ad hoc.\n\n## The brand constellation (verified, v4.1.0)\n\nEvery brand owns a mineral and a voice. Theme each brand's surfaces and copy in **its own** mineral\nand register — never default everything to tanzanite.\n\n| Brand        | Role                        | Mineral    | Meaning    | Voice                                  |\n| ------------ | --------------------------- | ---------- | ---------- | -------------------------------------- |\n| **bundu**    | The ecosystem               | copper     | Wilderness | visionary, grounded, inclusive         |\n| **nyuchi**   | Infrastructure & enterprise | gold       | Bee        | technical, reliable, industrious       |\n| **mukoko**   | Africa's super app          | tanzanite  | Beehive    | welcoming, structured, protective      |\n| **shamwari** | Sovereign AI companion      | sodalite   | Friend     | helpful, warm, intelligent             |\n| nhimbe       | Events & gatherings         | malachite  | Gathering  | celebratory, communal, vibrant         |\n| bushtrade    | Marketplace                 | gold       | Bush trade | practical, trustworthy, local          |\n| lingo        | Language learning           | cobalt     | Language   | encouraging, cultural, playful         |\n| campfire     | Platform messaging anchor   | malachite  | Campfire   | direct, warm, always present           |\n| bytes        | Short-form creator video    | tanzanite  | Bytes      | energetic, creative, youthful          |\n| novels       | Publishing platform         | malachite  | Novels     | literary, thoughtful, immersive        |\n| places       | Geographic knowledge graph  | gold       | Places     | authoritative, helpful, discoverable   |\n| transport    | Public transit & booking    | gold       | Transport  | efficient, reliable, practical         |\n| planner      | Productivity hub            | cobalt     | Planner    | organised, clear, supportive           |\n| wallet       | Payments & tokens           | gold       | Wallet     | trustworthy, precise, secure           |\n| pulse        | Feed & Mukoko Home          | tanzanite  | Pulse      | personal, adaptive, ambient            |\n| health       | Wellness & telemedicine     | malachite  | Health     | caring, accurate, private              |\n| circles      | Community messaging         | terracotta | Circles    | inclusive, moderated, community-driven |\n\nThe narrative spine: **nyuchi is the bee, mukoko is the hive, bundu is the wilderness the hive\nlives in.** Use this when explaining the ecosystem — it is the one-sentence architecture.\n\n## Ubuntu as identity (live doctrine)\n\nBundu carries the philosophy. _Ndiri nekuti tiri_ — I am because we are.\n\n**Five Pillars** (spheres of belonging): **family** (the household and kin, the primary web of\nbelonging) → **community** (neighbours, peers, chosen kin) → **society** (the broader human web —\nstrangers, institutions, the polity) → **environment** (the natural world that holds all\nrelationships; Ubuntu is ecological before it is social) → **spirituality** (the vertical\ndimension — meaning, purpose, ancestry, the sacred).\n\n**Five Principles** (operating rules): **survival** (collective endurance — no one survives alone) ·\n**solidarity** (firm, persevering commitment to the common good) · **compassion** (active care shown\nthrough helping, sharing, welcoming) · **respect** (unprejudiced consideration of another's\nprivileges, beliefs, norms — includes elder-respect) · **dignity** (inherent worth by virtue of\nexistence, irrespective of status or background).\n\nIn ecosystem materials, philosophy is **structural, not decorative**: name a pillar/principle only\nwhen the feature or message genuinely embodies it.\n\n## Rules for ecosystem-level work\n\n- **Lowercase wordmarks, always** — `bundu`, `nyuchi`, `mukoko`, `shamwari`, and every sister brand.\n- **Copper accents** signal \"this is the ecosystem speaking\" (parent-level pages, governance,\n  commons, manifesto-register material).\n- **Cross-brand layouts:** lead with the brand that owns the message; bring in the constellation\n  table's minerals when showing the family together (each brand in its own mineral — this is what\n  the Swarm mark's full palette expresses at logo scale).\n- **Voice shifts with the speaker.** A bundu page is visionary/grounded/inclusive; the same content\n  on mukoko.com becomes welcoming/structured/protective. Don't copy-paste tone across brands.\n- Cultural specificity is non-negotiable: Shona vocabulary and Zimbabwean grounding are structural.\n- All UI tokens (radius, type, spacing, surfaces, status colors) come from **nyuchi-design**;\n  Mukoko logo/assets come from **mukoko-design**. This skill governs _which brand speaks and how_.\n\n## If invoked with no other guidance\n\nAsk whether the work is (a) parent-brand material (bundu.org page, manifesto, investor/ecosystem\ndeck), (b) a cross-brand piece (family overview, comparison, announcement spanning brands), or\n(c) a voice/mineral decision for a specific brand — then apply the constellation table and the\nvoice rules, pulling tokens and assets from the sibling skills.\n\n_Bundu — the wilderness the hive lives in._",
    source: "mzizi-tools/mzizi-skills/skills/bundu-design",
  },
  {
    name: "nyuchi-design",
    description:
      "UI + tokens for the nyuchi / bundu / mukoko ecosystem: the Seven African Minerals colour system, named radius scale, Noto Sans/Serif + JetBrains Mono type, pill-button + dense control-scale rules, and a super-app UI kit. Use to generate well-branded interfaces.",
    body_mdx:
      "> **Doctrine v4.1.0.** The source of truth for all tokens is the live database\n> `nyuchi_design_db → component_documents` (and its mirror `globals.css` / `colors_and_type.css`).\n> Pull live before assuming — the palette and scales evolve faster than this file.\n> **Last verified (this revision):** minerals, heritage anchors, radius scale. Typography, spacing,\n> semantic colors, motion, shadow and z-index were **not** re-verified in this pass — confirm against the DB if precision matters.\n\nRead the `README.md` file within this skill, and explore the other available files. Key entry points:\n\n- `README.md` — brand context, content fundamentals, visual foundations, iconography.\n- `colors_and_type.css` — canonical CSS tokens (light + dark). Always `@import` this rather than hard-coding hex values. **Note:** refresh against the live DB for the two newest minerals (sodalite, copper) and the heritage anchors if the local file predates doctrine v4.1.0.\n- `assets/` — brand marks. **nyuchi** = the bee (worker / infrastructure). **mukoko** = the hive. Mukoko's current mark is **the Swarm** (seven-mineral Seed-of-Life cluster) — see the dedicated `mukoko-design` skill. Use the light mark on dark surfaces and vice versa.\n- `preview/` — reference cards for type, colors, spacing, components, brand.\n- `ui_kits/mukoko/` — React/JSX super-app UI kit with a working click-through prototype.\n\n## Rules that are non-negotiable\n\n- **Lowercase wordmarks** — `mukoko`, `nyuchi`, `bundu`, `shamwari`, `nhimbe`, `bushtrade` — always.\n- **Seven African Minerals** (geological, the core palette). Pull from `--color-<name>` / `--container-<mineral>` / `--on-container-<mineral>`:\n  - `cobalt` — primary blue, links, CTAs · digital future, trust\n  - `tanzanite` — **mukoko's brand mineral**, social features · premium, connection\n  - `malachite` — success states, positive actions · growth\n  - `gold` — achievements, rewards, highlights · honey, warmth\n  - `terracotta` — community features · earth, grounding\n  - `sodalite` — **AI / Shamwari surfaces, deep-reasoning states** · intelligence, depth _(added v4.1.0)_\n  - `copper` — **Bundu ecosystem identity, the commons** · connection, stewardship _(added v4.1.0)_\n- **Two Heritage anchors** (atmospheric neutrals, mini-app surfaces & backgrounds): `hematite` (#546E7A / #90A4AE, neutral anchor) and `kalahari` (#C9B589 / #E8D9B5, warm light anchor). _(The earlier \"Five Heritage / Ten Colors of Africa\" framing is retired.)_\n- **Each brand owns a mineral** (`styling-ecosystem`, verified v4.1.0): bundu→**copper**, nyuchi→**gold**, mukoko→**tanzanite**, shamwari→**sodalite**, nhimbe/campfire/novels/health→malachite, lingo/planner→cobalt, bushtrade/places/transport/wallet→gold, bytes/pulse→tanzanite, circles→terracotta. Theme a surface in its brand's mineral — never default everything to tanzanite.\n- **Buttons are ALWAYS pill** (`rounded-full` → `--radius-full`, 9999px). Inputs match buttons visually.\n- **Controls are dense by default:** `h-8` (32px) small · `h-9` (36px) default · `h-10` (40px) large. **56px (`h-14`) is reserved for specific prominent surfaces — hero CTAs and the like — it is not the default.** An earlier revision of this skill said 56px default / 48px minimum; the shipped primitives never did that, so the rule described a system that did not exist. Because the default sits below the 44pt (Apple HIG) and 48dp (Material) guidance, a dense control that is a **touch** target must earn its hit area another way — surrounding spacing, or padding the interactive area beyond the visual box. Badges are 20px tall.\n- **Radius scale is named** (px): `none` 0 · `xs` 4 · `sm` 7 · `md` 12 · `lg` 14 (**DEFAULT** cards/panels) · `xl` 17 · `2xl` 24 · `full` 9999 (pills). Checkboxes use `sm`(7), inputs/small cards `md`(12), cards/panels `lg`(14), modals/sheets/dialogs/tabs `xl`(17), hero cards `2xl`(24). Reference via `--radius-<name>`.\n- **H1–H3 are Noto Serif. H4+ and body are Noto Sans. Code is JetBrains Mono.**\n- **Rings over shadows.** Cards get `ring-1` of `--border`, not a drop shadow.\n- **No gradients, no glassmorphism** except sticky chrome (`backdrop-blur`).\n- **Emoji only as mini-app identifiers**, never as chrome or body decoration.\n- **Status colors are NOT minerals.** Use `--status-success` (#22C55E), `--status-warning` (#F59E0B), `--status-error` (#EF4444), `--status-info` (#3B82F6) — universal accessibility-first. Severity scale (`--severity-low/moderate/high/severe/extreme/cold`) and connection (`--connection-online/syncing/cached/offline`) follow the same logic. Verification tiers are the only place minerals carry status meaning (community→malachite, otp→cobalt, government→tanzanite, licensed→gold).\n- **Surfaces (April 2026 AAA refresh):** light mode — `--background` warm paper, `--card` pure white, `--muted` cream, `--overlay` white. Dark mode — `--background` warm stone L10%, `--card` L6% (darker than bg), `--muted` L2% (deepest), `--overlay` L14% (lighter than bg, scrim creates pop).\n- **Mukoko's mark is the Swarm** — a seven-mineral Seed-of-Life cluster (tanzanite core, six minerals ringed around it). Full-palette at ≥32px; mono single-mineral version below that. Full kit + rules live in the `mukoko-design` skill.\n- **Lucide React** for UI icons (or the Lucide CDN JS build for HTML prototypes).\n- **APCA 3.0 AAA contrast** on both primary and secondary text on every surface.\n\n## When creating visual artifacts\n\nCopy assets out of this skill and create static HTML files for the user to view. `@import` `colors_and_type.css` at the top of every HTML file. For phone prototypes copy the structure from `ui_kits/mukoko/index.html`. For slide decks use the MineralStrip accent and serif display type.\n\n## When working on production code\n\nInstall upstream components with the shadcn CLI — never hand-roll copies:\n\n```bash\nnpx shadcn@latest add https://mzizi.dev/api/v1/ui/<name>\n```\n\nRead the rules in `README.md` to become an expert in designing with this brand. Pull CSS variables from the upstream `globals.css` (mirrored in `colors_and_type.css`); refresh the mineral/heritage/radius tokens from the live DB if the mirror predates v4.1.0.\n\n## If invoked with no other guidance\n\nAsk what the user wants to build or design (marketing page? a new mini-app for mukoko? a deck? a component?), ask what surface (mobile / desktop / print / slide), ask which brand voice (bundu / nyuchi / mukoko / shamwari / nhimbe), then act as an expert designer who outputs HTML artifacts _or_ production code, depending on the need.",
    source: "mzizi-tools/mzizi-skills/skills/nyuchi-design",
  },
  {
    name: "mukoko-design",
    description:
      "Mukoko brand identity: the seven-mineral Seed-of-Life Swarm mark, the full mineral palette with meanings, ready-made SVG/PNG assets (logos, app icons, favicons, lockups, honeycomb backgrounds), and design tokens. Use when creating or exporting Mukoko visual identity.",
    body_mdx:
      "# Mukoko — _The Swarm_\n\nThe visual identity for **Mukoko**, the hive at the centre of the Nyuchi ecosystem. Every asset\nhere is generated from the live tokens in `nyuchi_design_db → styling-minerals` (doctrine **v4.1.0**).\n**The database is the source of truth**, not this folder — if the palette shifts, re-pull and regenerate.\n\n## The idea (the meaning, say it right)\n\nThe mark is the **Seed of Life** — one centre cell ringed by six, the first complete ring of the\nhoneycomb — rendered in the **seven African Minerals**:\n\n> **Tanzanite at the core** (Mukoko, the brand), ringed by the six minerals of the ecosystem.\n> One hive holding the whole. **Ndiri nekuti tiri** — _I am because we are._\n\nIt is a _known_ geometric figure (like the Audi or Olympic rings), made ours through the mineral\npalette and the meaning. Symmetry of form; distinctiveness through colour and story. **nyuchi is the\nbee; mukoko is the hive** — so Mukoko's hero mark is the hive/cluster, never a bee (that's Nyuchi's).\n\n## The seven minerals\n\n| Mineral       | Light     | Dark      | Meaning                 | Role                  |\n| ------------- | --------- | --------- | ----------------------- | --------------------- |\n| cobalt        | `#0047AB` | `#00B0FF` | digital future, trust   | CTAs, links           |\n| **tanzanite** | `#4B0082` | `#B388FF` | premium, connection     | **brand / core cell** |\n| malachite     | `#004D40` | `#64FFDA` | growth, success         | positive actions      |\n| gold          | `#5D4037` | `#FFD740` | honey, rewards          | achievements          |\n| terracotta    | `#A0522D` | `#E1B07E` | earth, community        | community             |\n| sodalite      | `#283593` | `#3D5AFE` | intelligence, depth     | AI / Shamwari         |\n| copper        | `#BF5A36` | `#FF8A65` | connection, stewardship | Bundu / commons       |\n\nUse `light` on light surfaces, `dark` on dark. Full machine-readable values (containers + on-containers)\nare in `tokens/minerals.json`; drop-in CSS custom properties are in `tokens/minerals.css`.\n\n**Ring order** (clockwise from top): `cobalt → gold → malachite → copper → sodalite → terracotta`,\n**tanzanite** in the centre. This alternates cool/warm for visual balance — a design decision, not a\nlaw. If positions are ever assigned meaning, regenerate from the geometry script.\n\n## Assets in this skill\n\n```\nlogo/mark/        full-palette mark (light/dark, SVG + PNG) + mono/ (7 minerals × light|dark SVG)\nlogo/lockup/      mark + lowercase `mukoko` serif wordmark (light/dark, SVG + PNG)\napp-icon/         primary (full palette on deep tanzanite), paper, mono-tanzanite knockout (SVG + 1024 PNG)\nfavicon/          MONO tanzanite mark, legible small (SVG + 16/32/48/180 PNG)\nbackgrounds/      honeycomb grain (paper), tanzanite header band, stone splash 1080×1920, full-mineral pattern\ntokens/           minerals.json (source for native tokens) + minerals.css (web, auto dark-mode)\nBRAND-GUIDE.md    the full written guide (meaning, manifest, usage, do/don't)\n```\n\n## Rules for using the mark\n\n- **Default to the full-palette mark at ≥32px** (headers, About screens, decks, profile images, merch).\n- **Below 32px use the mono favicon** — the seven hues fuse small; the single-mineral version stays crisp. This is why the favicon ships mono tanzanite.\n- **Mono variants** let one app/surface/sub-brand wear a single mineral (Shamwari→sodalite, community→terracotta, success→malachite). Shape constant; only colour changes.\n- **Clear space** = one cell-radius on all sides. **Min size:** 24px full mark, 16px mono favicon.\n- **App icon default** = `app-icon/mukoko-appicon-primary` (full palette on deep tanzanite `#1A0033`).\n- **Wordmark:** Noto Serif 600, **lowercase always** (`mukoko`, never `Mukoko`/`MUKOKO`). UI = Noto Sans; code/labels = JetBrains Mono.\n\n## Do / don't\n\n**Do** — use the supplied SVGs; flip light/dark mineral values to the surface; use mono variants to signal context; give the mark room.\n**Don't** — add gradients/shadows/glows/outlines; recolour petals off-palette or reorder the ring arbitrarily; rotate, stretch or rearrange cells; capitalise the wordmark; use the full-palette mark below 32px.\n\n## Regenerating\n\nAll assets are deterministic from the minerals tokens. Re-pull `styling-minerals` from the DB and\nrebuild the cluster (core + six petals at edge-normal angles `30 + 60·i`, distance `√3·r + gap`,\nsharing true edges) so geometry, ring order and exports stay reproducible.\n\n## If invoked with no other guidance\n\nAsk what's needed (logo export? app icon? social/launch asset? splash? a new surface wearing a\nmineral?), confirm light vs dark surface, then produce the asset using the files here — or regenerate\nfrom tokens for a size/format not already exported.\n\n_Mukoko — the hive that holds the whole._",
    source: "mzizi-tools/mzizi-skills/skills/mukoko-design",
  },
  {
    name: "scaffold-component",
    description:
      "How to scaffold a new Mzizi component correctly: choose the right node (N1-N11), follow the per-node criteria checklist for N2/N3/N6/N8, author source into the component_documents document store, add docs and demos, promote alpha to stable, and validate contrast before ship. Covers the collection + node + owner columns, pill-shape rules, data-slot/data-portal requirements, harness integration, and the DB-first authoring pattern.",
    body_mdx:
      "# Scaffolding a Mzizi component\n\nFollow this workflow whenever you are creating a new component in the Mzizi design system.\nThe system is **DB-first**: production source lives in the Supabase document store, not in the\nfile system. The portal pulls from the database at build time.\n\n> `public.components` is a **read-only view** projected from the `component_documents` base\n> table — you cannot INSERT or UPDATE it. Author by writing a row to `component_documents`;\n> the view reflects it automatically.\n\n## Step 1 — Decide the node\n\nThe architecture is the **Mzizi DNA double helix**: nodes sit on one of two backbones\n(engineering / meaning), and rungs bridge both. The N-numbers are **labels, not a sequence**.\n\n| Node              | What belongs there                                                                        |\n| ----------------- | ----------------------------------------------------------------------------------------- |\n| N1 tokens         | A CSS value — colour, spacing, motion, radius (a `styling-*` collection, not a component) |\n| N2 primitive      | Generic UI — button, card, input, dialog, toolbar, bento-grid                             |\n| N3 brand          | A branded composition with the mineral palette and harness integration                    |\n| N4 safety         | A conditional-rendering gate — permission, geo, rate limit                                |\n| N5 resilience     | Error boundary, skeleton, offline banner, fallback chain                                  |\n| N6 pages          | Full-screen layout composition                                                            |\n| N7 shell          | App container — navigation, routing, lifecycle                                            |\n| N8 assurance      | Instrumentation, a11y audit, RTL conformity, probes                                       |\n| N9 fundi          | Automated healing (a rung — owned by `mzizi-tools`)                                       |\n| N10 documentation | Docs, AI instructions, docs infrastructure (a rung)                                       |\n| N11 discovery     | SEO / AIO surfaces — if the machine can't see it, it doesn't exist (a rung)               |\n\nIf you are unsure, query the MCP: `get_node_categories(N)` shows what already lives in each\nnode, and `get_node_documents` returns the live node/strand model. Never hardcode counts.\n\n## Step 2 — Register in `component_documents` with `status = alpha`\n\n```sql\nINSERT INTO component_documents (collection, name, node, owner, document)\nVALUES (\n  'primitives',              -- primitives | brand | pages | shell | safety | resilience | observability | fundi | documentation\n  'your-component-name',\n  2,                         -- ecosystem node; matches the collection\n  'mzizi',                   -- owner: mzizi | nyuchi | bundu | framework\n  jsonb_build_object(\n    'name',         'your-component-name',\n    'node',         2,\n    'collection',   'primitives',\n    'owner',        'mzizi',\n    'nodeLabel',    'primitive',     -- primitive/brand/safety/resilience/pages/shell/assurance/fundi/documentation\n    'category',     'layout',        -- e.g. layout, form, feedback, navigation, conformity\n    'status',       'alpha',         -- alpha until source is production-ready, then stable\n    'dnaRole',      'core-guarantee',-- core-guarantee | shipped | swappable | spine | genetic-code | transcription\n    'model',        'mzizi-dna-helix',\n    'framework',    'react',\n    'runtimeLang',  'typescript',\n    'platforms',    jsonb_build_array('web'),\n    'description',  'Short description of what it does',\n    'source_code',  '',              -- filled in Step 3\n    'urls', jsonb_build_object(\n      'portal',     'https://mzizi.dev/components/your-component-name',\n      'api',        'https://mzizi.dev/api/v1/ui/your-component-name',\n      'health',     'https://mzizi.dev/api/health/your-component-name',\n      'source',     'https://mzizi.dev/source/your-component-name',\n      'changelog',  'https://mzizi.dev/changelog/your-component-name',\n      'playground', 'https://mzizi.dev/playground/your-component-name'\n    )\n  )\n);\n```\n\nThe composite primary key is `(collection, name)`. `dna_role` and `schema_version` are derived\nfrom the document's `dnaRole` / `_schemaVersion`. Portal URLs stay on `mzizi.dev` (the\nregistry/portal host); the standalone MCP endpoint is `mcp.mzizi.dev/mcp`.\n\nSet `doctrineVersion` to the current doctrine revision — read it live rather than copying a\nnumber from this file.\n\n## Step 3 — Write the source\n\nOnce registered, write the production source into the document's `source_code` field via SQL —\nnever to the read-only `components` view.\n\n```sql\nUPDATE component_documents\nSET document = jsonb_set(document, '{source_code}', to_jsonb($$ ...your TSX source... $$::text), true)\nWHERE collection = 'primitives' AND name = 'your-component-name';\n```\n\nFollow the criteria for the chosen node.\n\n### N2 primitive checklist\n\n- No `useNyuchiHarness` import\n- `data-slot` attribute on the root element\n- `data-portal` attribute pointing at `mzizi.dev/components/{name}`\n- `cn()` for className composition\n- No raw Tailwind colours — use semantic tokens (`bg-primary`, `text-foreground`, …)\n- Icons from `@/lib/icons`, never `lucide-react` directly\n- **Control height and touch target are two different measurements — don't collapse them.** The visual box follows the dense scale (`h-8` 32px small · `h-9` 36px default · `h-10` 40px large); the hit area follows `styling-touch-targets` (`comfortable` 47 · `accessible` 43 · `minimum` 41, with spacing). A 36px-tall button reaches a 43–47px target through padding, not by growing. `generous` (56) is **hero CTAs only** — the collection says so in its own `usage` field\n- Pill-shaped categories (button, input, avatar, badge, toggle) use `rounded-full` (9999)\n\n### N3 brand checklist\n\n- `nyuchi-` prefix on the name\n- `useNyuchiHarness` with the full destructure `{ log, motion, LiveRegion }`\n- `animStyle` guarded by a `motion.prefersReduced` check\n- An ARIA role or `aria-label`\n- `data-slot` and `data-portal` attributes\n- `focus-visible` ring on interactive elements\n- A dense control that is a **touch** target earns its hit area through surrounding spacing or padding beyond the visual box — the 36px default sits below the 44pt/48dp guidance, so the spacing is what makes it tappable\n- I18N via `Intl` formatters for dates, numbers, currency\n- Semantic tokens for status-bearing colours\n\n### N6 page checklist\n\n- Pure composition — no inline buttons, cards, or SVGs\n- Accept children/slots for content\n- Semantic CSS vars only (`bg-card`, `text-foreground`, `bg-primary`)\n- A loading-state prop\n- `role=\"main\"` and an `aria-label`\n\n### N8 assurance checklist\n\n- A TypeScript module exporting typed functions and React hooks\n- A banner comment identifying the node and purpose\n- A configurable rules array\n- Optional `onViolation` / `onComplete` callbacks\n- A React hook variant for continuous monitoring\n\n## Step 4 — Add docs and demos\n\nDocs and demos are fields **inside** the same `component_documents` row — not separate tables.\n\n```sql\nUPDATE component_documents\nSET document = document || jsonb_build_object(\n  'use_cases', jsonb_build_array('Use case 1', 'Use case 2'),\n  'variants',  jsonb_build_array('default', 'sm', 'lg'),\n  'features',  jsonb_build_array('cn() for className composition', 'data-slot attribute'),\n  'a11y',      jsonb_build_array('Accessibility feature'),\n  'demo',      jsonb_build_object('has_demo', true, 'demo_type', 'interactive')\n)\nWHERE collection = 'primitives' AND name = 'your-component-name';\n```\n\n## Step 5 — Flip status to stable\n\nOnce the source is production-ready and has been dogfooded somewhere:\n\n```sql\nUPDATE component_documents\nSET document = jsonb_set(document, '{status}', '\"stable\"')\nWHERE collection = 'primitives' AND name = 'your-component-name';\n\nSELECT log_version('your-component-name', 'promoted', 'Promoted from alpha to stable', 'your-handle');\n```\n\n## Step 6 — Validate accessibility\n\nIf your component introduces a new colour pair, validate before shipping:\n\n```sql\nSELECT calculate_contrast_ratio('#FFFFFF', '#0047AB');\n-- Returns the WCAG 2.1 contrast ratio. AA 4.5, AAA 7.0.\n```\n\nFor systematic pair tracking see `styling-accessibility-checks` — critical pairs have a row\nwith a computed ratio and colour-blindness safety flags.\n\n## Step 7 — Sync the committed snapshot\n\nIn the portal repo, regenerate and commit the registry snapshot so the PR shows the delta:\n\n```bash\npnpm registry:sync     # rewrites registry.json (+ components/ui/* if portal-imported)\npnpm registry:verify   # the CI drift gate\n```\n\n## What to avoid\n\n- Never store component source in the file system if it is meant to be distributed via the\n  registry. The document store is canonical.\n- Never hardcode hex colour values (except documented third-party brands like Ethereum,\n  Google, EcoCash).\n- Never use `lucide-react` directly — go through `@/lib/icons`.\n- Never use `margin-left` / `padding-right` / `left` — use logical properties\n  (`margin-inline-start`, …) for RTL support.\n- Never bump a major version on your own initiative. The portal's code SemVer and the\n  doctrine/IP line are independent tracks, and a major on either is a maintainers' call.",
    source: "mzizi-tools/mzizi-skills/skills/scaffold-component",
  },
  {
    name: "ecosystem-app-setup",
    description:
      "End-to-end setup for a new bundu-ecosystem app: Next.js bootstrap, Mzizi token wiring, components.json against the registry, theme-provider setup, installing the skills bundle and the fundi agent, and the Ubuntu constraints that decide what belongs in the ecosystem.",
    body_mdx:
      '# Setting up a new bundu-ecosystem app\n\nFollow this skill when someone wants to start a new app that is part of the bundu ecosystem\n(mukoko, nyuchi, shamwari, bundu, nhimbe) or an app that consumes the Mzizi design system.\n\n## 1. Create the project\n\n```bash\npnpm create next-app@latest my-app --turbopack --typescript --tailwind --app --no-src-dir\ncd my-app\n```\n\n## 2. Install the Mzizi-compatible dependencies\n\n```bash\npnpm add class-variance-authority clsx tailwind-merge\npnpm add -D @tailwindcss/typography\n```\n\n## 3. Wire the tokens\n\nMzizi tokens are **theme-adaptive CSS custom properties**. Add the Seven African Minerals to\n`app/globals.css` in both `:root` and `.dark`, and register them in the Tailwind 4 `@theme`\nblock so utilities generate.\n\n```css\n:root {\n  /* Seven minerals — light values. deep-earth family */\n  --color-cobalt: #0047ab; /* Knowledge  */\n  --color-tanzanite: #4b0082; /* Identity   */\n  --color-malachite: #004d40; /* Growth     */\n  --color-sodalite: #283593; /* Intelligence */\n  /* hand family */\n  --color-gold: #5d4037; /* Value       */\n  --color-terracotta: #a0522d; /* Community   */\n  --color-copper: #bf5a36; /* Stewardship */\n}\n\n.dark {\n  --color-cobalt: #00b0ff;\n  --color-tanzanite: #b388ff;\n  --color-malachite: #64ffda;\n  --color-sodalite: #3d5afe;\n  --color-gold: #ffd740;\n  --color-terracotta: #e1b07e;\n  --color-copper: #ff8a65;\n}\n```\n\n**These are a seed snapshot, not the source of truth.** Pull the canonical light/dark,\ncontainer and role values live before you rely on them:\n\n```bash\ncurl -s https://mzizi.dev/api/v1/brand | jq \'.minerals\'\n```\n\nor over the MCP: `get_brand_tokens` / `list_brand_token_categories`. The palette also carries\nseven heritage tones, the status set, and the computed Experimental Seven — see the\n`nyuchi-design` skill for the full system.\n\nPick the app\'s accent through `--brand-accent` rather than hardcoding a mineral hex: nyuchi =\ngold, mukoko = tanzanite.\n\n## 4. Point `components.json` at the Mzizi registry\n\n```json\n{\n  "$schema": "https://ui.shadcn.com/schema.json",\n  "style": "new-york",\n  "tailwind": {\n    "config": "tailwind.config.ts",\n    "css": "app/globals.css",\n    "baseColor": "neutral",\n    "cssVariables": true\n  },\n  "aliases": {\n    "components": "@/components",\n    "utils": "@/lib/utils",\n    "ui": "@/components/ui"\n  },\n  "registries": {\n    "mzizi": {\n      "url": "https://mzizi.dev/api/v1/ui"\n    }\n  }\n}\n```\n\n## 5. Install components to verify\n\n```bash\npnpm dlx shadcn@latest add https://mzizi.dev/api/v1/ui/nyuchi-theme-provider\npnpm dlx shadcn@latest add https://mzizi.dev/api/v1/ui/button\n```\n\nNever copy-paste component source between apps — install from the registry so every app\ninherits fixes.\n\n## 6. Wire the theme provider\n\nIn `app/layout.tsx`:\n\n```tsx\nimport { NyuchiThemeProvider } from "@/components/nyuchi-theme-provider"\n\nexport default function RootLayout({ children }) {\n  return (\n    <html lang="en" suppressHydrationWarning>\n      <body>\n        <NyuchiThemeProvider>{children}</NyuchiThemeProvider>\n      </body>\n    </html>\n  )\n}\n```\n\n## 7. Give the app\'s AI assistants the doctrine\n\nInstall the published skills bundle so any assistant working in the repo has the design system\non hand:\n\n```bash\nnpx skills add @nyuchi/mzizi-skills\n```\n\nOr install the whole toolchain as a Claude Code plugin — skills, the `fundi` agent, the\nregistry MCP, and the `/mzizi:*` commands in one step:\n\n```\n/plugin marketplace add nyuchi/mzizi-tools\n/plugin install mzizi@mzizi-tools\n```\n\nTo wire the registry MCP directly instead, add it to `.mcp.json` / your client config:\n\n```json\n{\n  "mcpServers": {\n    "mzizi": { "type": "http", "url": "https://mcp.mzizi.dev/mcp" }\n  }\n}\n```\n\n## 8. Let fundi do the wiring for you\n\n`@nyuchi/mzizi-cli` ships the **fundi** agent, which reads the project off disk and plans the\nscaffolding against the live registry:\n\n```bash\npnpm add -D @nyuchi/mzizi-cli\nnpx fundi explore                       # offline — snapshot the project\nnpx fundi plan "wire up Mzizi tokens and install the theme provider"\n```\n\n`explore` runs without credentials; `plan` and `chat` need `ANTHROPIC_API_KEY`. A plan is\ninert until you apply it, so read it before letting it write.\n\n## 9. Verify\n\n```bash\npnpm dev\n```\n\nConfirm the theme toggle works, the button renders with mineral-based styling, and Tailwind\npicks up the CSS variables.\n\n## Ubuntu alignment\n\nThe ecosystem is built on Ubuntu — "I am because we are." When you architect a new app:\n\n- Prefer open source over proprietary\n- Build for community, not extraction\n- Respect data sovereignty — African data stays on African infrastructure where possible\n- Accessibility is not an afterthought; it is the starting point\n- Every feature should contribute to collective wellbeing, not just individual efficiency\n\nThese aren\'t marketing claims, they\'re architectural constraints. If you find yourself\ndesigning something that only benefits a small subset of users at others\' expense, question\nwhether it belongs in the ecosystem.\n\n## What is and isn\'t an ecosystem app\n\nThe ecosystem has two legal layers. The **Bundu Foundation** (research lab, standards body,\nopen-source org; governs the doctrine, the architecture, and Mzizi) sits above **Nyuchi Africa**\n(the operating company). Inside Nyuchi Africa sit three pillars and the platform:\n\n- **mukoko** — the consumer pillar: the super-app (social, commerce, payments, identity)\n- **nyuchi** — the commercial/infrastructure pillar and the platform (web services, developer\n  tools, MCP, storage)\n- **shamwari ai** — the intelligence pillar, sovereign by nature (on-device inference,\n  pod-resident context, no third-party AI vendor)\n- **nhimbe** — cooperative economic features\n\nBundu is not an app and not a layer in a stack; it is the Foundation that governs the\necosystem. mukoko and shamwari ai are pillars inside Nyuchi Africa, not separate companies.\n\nNot ecosystem apps:\n\n- Any app that extracts value without giving back\n- Any app that requires surveillance for its business model\n- Any app that centralises what should be federated\n\nIf the app you\'re building doesn\'t fit, that\'s fine — consume the Mzizi design system, but\ndon\'t brand it as ecosystem.\n\n## Wordmarks\n\nAll brand wordmarks are lowercase: `mzizi`, `mukoko`, `nyuchi`, `shamwari`, `bundu`, `nhimbe`,\n`fundi`.',
    source: "mzizi-tools/mzizi-skills/skills/ecosystem-app-setup",
  },
  {
    name: "cloudflare-worker-rust",
    description:
      "Build a nyuchi Cloudflare Worker in Rust with workers-rs: project layout, wrangler config, cron triggers and queue consumers, the Supabase access pattern, WorkOS token validation by audience, logging conventions, and deploy. Covers when Rust is the right choice versus the TypeScript worker stack.",
    body_mdx:
      '# Cloudflare Worker in Rust — the nyuchi pattern\n\n`workers-rs` compiles Rust to WebAssembly and runs it on the Cloudflare Worker runtime. This\nskill is the house pattern for nyuchi services written that way.\n\n## Pick the language before you pick the pattern\n\nBoth stacks are in production, and the choice is not stylistic:\n\n- **Rust (`workers-rs`)** — CPU-bound work, strict correctness requirements, or a service that\n  already shares Rust crates with its siblings. The Nyuchi Console (`nyuchi/mukoko-platform`)\n  runs Rust workers.\n- **TypeScript** — anything that leans on the MCP SDK, `hono`, `agents`, Durable Objects, or\n  the Supabase JS clients. Every Worker in `nyuchi/mzizi-tools` (`mzizi-mcp`, `fundi-tester`,\n  `bushtrade-mcp`) is TypeScript for exactly this reason: the ecosystem\'s MCP and agent\n  libraries are TS-first.\n\nIf you are adding an MCP server or an agent surface, you almost certainly want TypeScript —\nsee the `mcp-server-cloudflare` skill. Reach for Rust when the workload, not the taste,\njustifies it.\n\n## Project setup\n\n```bash\nnpm install -g wrangler\ncargo generate cloudflare/workers-rs\n```\n\n```\nservice-name/\n├── src/\n│   ├── lib.rs          # Worker entry points\n│   ├── router.rs       # Request routing\n│   ├── supabase.rs     # Supabase access\n│   ├── auth.rs         # WorkOS token validation\n│   └── handlers/       # One file per route\n├── wrangler.toml\n└── Cargo.toml\n```\n\n## wrangler.toml\n\n```toml\nname = "nyuchi-example-service"\nmain = "build/worker/shim.mjs"\ncompatibility_date = "2026-01-01"\n\n[build]\ncommand = "cargo install -q worker-build && worker-build --release"\n\n# Secrets — set out-of-band with `wrangler secret put <NAME>`, never committed:\n#   SUPABASE_URL\n#   SUPABASE_SECRET_KEY\n#   WORKOS_M2M_CLIENT_ID\n#   WORKOS_M2M_CLIENT_SECRET\n\n[[triggers.crons]]\ncron = "*/5 * * * *"\n\n[[queues.consumers]]\nqueue = "nyuchi-example-signals"\nmax_batch_size = 10\n```\n\nKeep `compatibility_date` current; a stale date silently pins you to old runtime behaviour.\n\n## Entry points\n\n```rust\n// src/lib.rs\nuse worker::*;\nmod router;\nmod supabase;\nmod auth;\n\n#[event(fetch)]\npub async fn main(req: Request, env: Env, _ctx: Context) -> Result<Response> {\n    router::handle(req, env).await\n}\n\n// Cron trigger\n#[event(scheduled)]\npub async fn scheduled(_event: ScheduledEvent, env: Env, _ctx: ScheduleContext) {\n    if let Err(e) = jobs::run_cycle(&env).await {\n        console_error!("[nyuchi:example] cycle error: {:?}", e);\n    }\n}\n\n// Queue consumer\n#[event(queue)]\npub async fn queue(batch: MessageBatch<String>, env: Env, _ctx: Context) -> Result<()> {\n    jobs::process_signals(batch, &env).await\n}\n```\n\n## Supabase access\n\nTwo routes, and the choice matters for latency:\n\n- **PostgREST over `fetch`** — no extra infrastructure, works from any Worker, and is what the\n  snippet below does. Fine for a handful of reads per request.\n- **Hyperdrive** — globally-pooled native Postgres, so no TCP handshake per request and the\n  full SQL function surface is available. Worth it once a service is chatty or needs RPCs.\n\n```rust\n// src/supabase.rs\npub struct SupabaseClient {\n    url: String,\n    secret_key: String,\n}\n\nimpl SupabaseClient {\n    pub fn from_env(env: &Env) -> Result<Self> {\n        Ok(Self {\n            url: env.secret("SUPABASE_URL")?.to_string(),\n            secret_key: env.secret("SUPABASE_SECRET_KEY")?.to_string(),\n        })\n    }\n\n    pub async fn get(&self, table: &str, query: &str) -> Result<String> {\n        let url = format!("{}/rest/v1/{}?{}", self.url, table, query);\n        let mut headers = Headers::new();\n        headers.set("apikey", &self.secret_key)?;\n        headers.set("Authorization", &format!("Bearer {}", self.secret_key))?;\n        headers.set("Content-Type", "application/json")?;\n\n        let req = Request::new_with_init(&url, RequestInit::new().with_headers(headers))?;\n        let mut resp = Fetch::Request(req).send().await?;\n        resp.text().await\n    }\n}\n```\n\nA service-role key bypasses RLS. Only reach for it on a server-side write path; anything\nserving public reads should use the anon key and let RLS do the gating.\n\n## Auth — decide by audience\n\nAuthentication follows the ecosystem\'s auth-by-audience doctrine:\n\n- **Public / customer surfaces** → WorkOS **Connect OAuth PKCE** (public client, no secret).\n  Entitlement is a commerce subscription, never a per-user tag.\n- **Internal staff / machine surfaces** → WorkOS **M2M** (client-credentials). Only the Worker\n  holds the secret; CLIs and browsers never do. Per-route scopes come from the token claims.\n\nValidate the inbound token against the WorkOS JWKS before doing any privileged work, and cache\nthe JWKS — refetching per request is the classic latency bug:\n\n```rust\n// src/auth.rs\npub struct Claims { pub sub: String, pub scopes: Vec<String> }\n\npub async fn validate_token(req: &Request, env: &Env) -> Result<Option<Claims>> {\n    let Some(header) = req.headers().get("Authorization")? else {\n        return Ok(None); // no token — the caller decides whether that is allowed\n    };\n    let token = header.strip_prefix("Bearer ").unwrap_or(&header);\n\n    // 1. Fetch (and cache) the JWKS for the WorkOS environment.\n    // 2. Verify signature, `exp`, `iss`, and `aud`.\n    // 3. Map the verified claims onto `Claims`.\n    verify_against_jwks(token, env).await\n}\n```\n\nReturn `401` on an invalid token and `403` on a valid token missing the required scope — never\ncollapse the two, or callers can\'t tell "log in" from "you can\'t do this".\n\nRoutes that are not user-triggered — cron, queue consumers, health — bypass the gate by design.\n\n## Logging convention\n\nEvery log line carries a `[nyuchi:service-name]` prefix so it is greppable across services:\n\n```rust\nconsole_log!("[nyuchi:example] cycle complete: {} records processed", count);\nconsole_error!("[nyuchi:example] upstream error: {:?}", e);\n```\n\nNever log a token, a secret, or a full request body.\n\n## Deploy\n\n```bash\nwrangler dev        # local\nwrangler deploy     # production\n\nwrangler secret put SUPABASE_URL\nwrangler secret put SUPABASE_SECRET_KEY\nwrangler secret put WORKOS_M2M_CLIENT_ID\nwrangler secret put WORKOS_M2M_CLIENT_SECRET\n```\n\nPrefer wiring deploys to CI (a workflow on push to `main`, or Cloudflare Workers Builds) over\ndeploying from a laptop, so what is live always matches what is merged.',
    source: "mzizi-tools/mzizi-skills/skills/cloudflare-worker-rust",
  },
  {
    name: "mcp-server-cloudflare",
    description:
      "Add or change an MCP server running as a Cloudflare Worker in the bundu ecosystem: the live endpoint map, one-factory/many-entrypoints layout, Streamable HTTP transport, registry-driven tool catalogues with drift gates, tool naming and annotations, the auth-by-audience gate (Connect OAuth PKCE vs M2M), and publishing to the MCP registry.",
    body_mdx:
      '# MCP server on Cloudflare Workers — the ecosystem pattern\n\n## The live endpoint map\n\nRead this before inventing a hostname — each MCP has its own host, and the umbrella\n`mcp.nyuchi.dev/{service}` path scheme is **not** how these are deployed:\n\n| Endpoint              | Worker            | Serves                                                          |\n| --------------------- | ----------------- | --------------------------------------------------------------- |\n| `mcp.mzizi.dev/mcp`   | `mzizi-mcp`       | The Mzizi registry — components, tokens, architecture, doctrine |\n| `mzizi.dev/mcp`       | portal route      | The canonical document-route MCP inside the Mzizi portal        |\n| `docs.nyuchi.com/mcp` | `nyuchi-docs-mcp` | The docs corpus — search / ask / read / feedback                |\n| (Worker)              | `bushtrade-mcp`   | Agentic commerce — task-driven UCP/AP2 verbs                    |\n\n`design.nyuchi.com` is a 308 permanent redirect to `mzizi.dev`. Never reference it in new code\nor docs.\n\n## Transport\n\nMCP over HTTP is **Streamable HTTP**, JSON-RPC 2.0:\n\n- `POST /mcp` — JSON-RPC requests\n- `GET /mcp` — SSE stream\n- `DELETE /mcp` — session cleanup\n- `OPTIONS /mcp` — CORS preflight\n\nA server exposes `tools/list` + `tools/call`, and optionally `resources/list` and\n`prompts/list`.\n\nUse `@modelcontextprotocol/sdk` — TypeScript is the right choice here, because the SDK, `hono`,\nand `agents` are all TS-first. Pin the SDK version deliberately: `agents` hard-pins\n`@modelcontextprotocol/sdk`, and letting your own declaration drift produces two incompatible\n`McpServer` classes in the type graph and a private-field mismatch at typecheck.\n\n## One factory, many entrypoints\n\nKeep the server definition free of any runtime binding, then wrap it per surface. This is what\nlets the same catalogue ship as a Worker, a generic fetch handler, and a local stdio bin:\n\n```\nsrc/\n├── server.ts   # createXMcpServer(deps) → McpServer   ← the only place tools are defined\n├── http.ts     # createXHttpHandler()   — any fetch runtime\n├── worker.ts   # Cloudflare Worker entry, bound to env\n└── stdio.ts    # bin for local clients / the MCP registry\n```\n\n## Prefer a registry-driven tool catalogue\n\nHardcoding a tool list means every new tool is a code change and a deploy. Build the catalogue\nat startup from a table instead, and dispatch by the row\'s `kind`:\n\n- `sql_function` → `supabase.rpc(...)`\n- `edge_function` → `supabase.functions.invoke(...)`\n- `source_table` → `supabase.from(...).select(...)`\n\nKeep only genuinely code-shaped tools (health, collection listing) in source.\n\n**Registry-driven surfaces need two gates**, because a table edit changes what the server\nexposes with no code change:\n\n1. An **offline suite** that connects a real MCP `Client` to the factory over an in-memory\n   transport and asserts the contract — the tool prefix, annotations, filtering, output schemas,\n   and dispatch routing.\n2. A **live drift gate** that reconstructs the public surface from the live registry and diffs\n   it against a committed manifest, failing when a tool was added, edited, or removed without\n   updating it. Run it before deploy.\n\n## Tool naming and annotations\n\nTools are `snake_case` and named for the action: `list_components`, `get_component`,\n`search_components`, `get_node_documents`, `get_brand_tokens`, `get_skill`.\n\nEvery tool carries a `title`, an `outputSchema`, and honest behaviour hints:\n\n- `readOnlyHint` — true only if the tool cannot mutate anything. An action-kind tool that runs\n  an audit is **not** read-only.\n- `destructiveHint` / `idempotentHint` / `openWorldHint` — set them deliberately; clients use\n  them to decide what to auto-approve.\n\nReturn `structuredContent` matching the declared `outputSchema` (array results wrapped under\n`rows`, scalars under `result`).\n\nNote the RPC parameter convention: registry functions take `p_`-prefixed params. Call\n`get_component` with `{ p_name }`, `search_components` with `{ p_query, p_limit }`. Getting the\nprefix wrong surfaces as "Could not find function public.get_component(name)".\n\n## Auth — decide by audience\n\n- **Public / customer MCPs** (`mzizi-mcp`, `bushtrade-mcp`) → **WorkOS Connect OAuth PKCE**,\n  a public client with no secret, each MCP with its own Connect app. Entitlement for paid\n  surfaces is a commerce subscription, never a per-user tag.\n- **Internal staff / machine surfaces** → **WorkOS M2M** (client-credentials). Only the Worker\n  holds the secret. A Worker that fronts an internal service mints the M2M token itself and\n  proxies the call, so a CLI never has to hold it.\n\nWrite-kind tools stay off the anonymous surface. If a public write is genuinely needed, keep it\nto an append-only sink on an explicit allow-list, running `SECURITY DEFINER` or self-gating —\nand enumerate those exceptions in one place in the source.\n\nAuth happens **inline in the client**, never by punting the user to a browser tab.\n\n## Static assets\n\nIf the Worker also needs to serve `llms.txt` or `robots.txt`, put them behind an `[assets]`\nblock so they resolve ahead of the OAuth handler; everything else falls through to the server.\n\n## Instrumentation\n\nWrap tool dispatch and emit an observability event on any tool error, fire-and-forget via\n`ctx.waitUntil` so it never blocks the response. Errors feed the fundi self-healing loop;\ndeliberately injected faults go to the chaos stream instead.\n\n## Publishing to the MCP registry\n\n`server.json` next to `package.json` is the source of truth, and\n`package.json#mcpName` **must** equal `server.json#name` or the registry refuses the publish.\nRegistry names look like `io.github.nyuchi/<server>`.\n\nRelease checklist:\n\n1. Bump the version everywhere it is stated — the version constant the server reports in\n   `serverInfo`, `package.json#version`, `server.json#version`, and\n   `server.json#packages[0].version`. A mismatch here is the most common failed publish.\n2. Tag and push. CI publishes to npm with provenance, then runs\n   `mcp-publisher login github-oidc` followed by `mcp-publisher publish` to reach the registry.\n   The login uses OIDC, so no registry secret is needed.\n\n## Deploy\n\n```bash\nwrangler dev\nwrangler deploy\nwrangler secret put SUPABASE_URL\nwrangler secret put SUPABASE_PUBLISHABLE_KEY\n```\n\nWire the deploy to CI on push to `main` so what is live matches what is merged, and run the\ndrift gate + a live smoke pass of every read tool before `wrangler deploy`.',
    source: "mzizi-tools/mzizi-skills/skills/mcp-server-cloudflare",
  },
];
